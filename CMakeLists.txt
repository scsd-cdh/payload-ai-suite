cmake_minimum_required(VERSION 2.8)
project( SpectralSuite )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( SpectralSuite SpectralSuite.cpp )
target_link_libraries( SpectralSuite ${OpenCV_LIBS} )
